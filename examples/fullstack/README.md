# fullstack desktop web app

This also shows how to communicate between the backend and the frontend using the webview's bind and eval methods.

To build:
```
cd frontend
livid deploy --using=../backend
./bundle/backend
```